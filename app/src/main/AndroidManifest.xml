<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.english.onlineenglishteacher">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/new_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:targetApi="n">
        <activity android:name=".ui.home.ViewNoteActivity" />
        <activity android:name=".ui.home.NotesActivity" />
        <activity android:name=".ui.test.QuizResultActivity" />
        <activity android:name=".ui.test.QuizActualActivity" />
        <activity android:name=".ui.test.QuizByLevelActivity" />
        <activity android:name=".ui.chat.PrivateChatActivity" />
        <activity android:name=".ui.video.VideosActivity" />
        <activity android:name=".ui.login.register.RegisterActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.login.login.LoginActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>